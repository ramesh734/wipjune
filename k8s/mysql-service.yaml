apiVersion: v1
kind: Service
metadata: 
  name: mysql-service
spec: 
  type: ClusterIP
  ports:
  - name: mysql
    port: 3306        # Combined into one item and changed to standard MySQL port
    targetPort: 3306  # The port your MySQL container is actually listening on
  selector:
    app: mysql
